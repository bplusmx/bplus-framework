# Local configuration.
default:
  suites:
    default:
      contexts:
        - FeatureContext:
          - admin
          - password

parameters:
  wp_users:
    admin:
      'password'
    luisabarca:
      'password'
